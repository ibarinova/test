# Properties controlling file renaming.
# Uncomment to override defaults.

# XPath expression pointing to a node in a Trisoft meta-information (.met) XML file containing the new file name
# emctroff.filenames.xpath = /ishobject/ishfields/ishfield[@name='FTITLE']

# A regular expression to match a file name part which should be stripped
# emctroff.filenames.strip.regex = ^([A-Za-z0-9-]+_){2}
